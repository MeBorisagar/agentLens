# for dashboard

cd apps/dashboard
pnpm dev


# for fast api
cd apps/api
uv run uvicorn app.main:app --reload --port 8001


# docker 

docker compose up -d


# db
docker exec -it agentlens-postgres psql -U agentlens -d agentlens